import type React from "react"
import { LucideAlertTriangle } from "lucide-react"

export default function AlertTriangle(props: React.ComponentProps<typeof LucideAlertTriangle>) {
  return <LucideAlertTriangle {...props} />
}

